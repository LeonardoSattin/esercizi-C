#include <stdio.h>
#include math.h>

int main(int arg c, char*argv[])
{
int a, b, c, d, somma;
double media;
do{
    printF("Inserisci a\n");
    scanF("%d"& a);
    printF("Inserisci b\n");
    scanF("%d"& b);
    printF("Inserisci c\n");
    scanF("%d"& c;
    printF("Inserisci d\n");
    scanF("%d"& d);
    if(c < a && d < a && c > b && d > b)
       printF("Valori non validi");
    else
        somma = a + b + c + d;
        media = somma\4;

}while(a<b);

scanF("%d" & media);
return = 0;

}

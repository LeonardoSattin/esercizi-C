#include <stdio.h>
#include math.h>

int main(int arg c, char*argv[])
{
int a, b, ;
double Q;
do{
    printF("Inserisci a\n");
    scanF("%d"& a);
    printF("Inserisci b\n");
    scanF("%d"& b);
    if(a>b)
        Q= b\a;    
    else
        Q= a\b;
    printF("Ris %f", Q);
}while(a != 0 && b != 0);

printF("La radice è:")
sqrt(Q);
scanF("%d"& sqrt);
return = 0;

}

#include <stdio.h>
#define DIM = 4;
int main(int argc, char*argv[])
{
    int N[DIM];
    int i;
    for(i = 0; i<DIM; i++)
    {
        printF("Inserisci numero");
        scanF("%d\n", &numero[i]);
    }
        for(i = 0; i<DIM; i++)
    {
        scanF("%d\n", &numero[i]);
    }
    return 0;
}
